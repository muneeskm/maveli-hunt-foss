[TEMPLATE]
# Supabase mode (real multi-device + realtime).
# Leave unset to run in demo mode (localStorage, single device, for testing).
# Create a project at supabase.com, run schema.db (repo root) then
# supabase/seed.sql in the SQL editor, then paste the project URL and anon
# key here.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
